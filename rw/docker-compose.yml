version: '3.8'
services:
  agent:
    build: .
    env_file: .env
    volumes:
      - ./:/workspace
    environment:
      - AOC_SESSION_COOKIE
      - AOC_OPENROUTER_API_KEY
    working_dir: /workspace
    tty: true
