version: '3.8'
services:
  agent:
    image: python:3.11-slim
    env_file: .env
    volumes:
      - ./:/workspace
    working_dir: /workspace
    command: /bin/sh -c "pip install --no-cache-dir -r requirements.txt && python agent.py"
    tty: true
